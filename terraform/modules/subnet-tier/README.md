This module directory demonstrates how repeated infrastructure patterns can be abstracted into reusable Terraform modules. Rather than modularizing the entire project, I kept the core architecture in the root configuration intentionally -- the goal was to write and understand every resource directly. This module exists to show I know how and when to reach for modules, not that I reach for them by default.
